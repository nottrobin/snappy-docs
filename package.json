{
  "scripts": {
    "clean": "rm -rf node_modules yarn-error.log css css *.log *.sqlite _site/ build/ .jekyll-metadata .bundle",
    "build": "documentation-builder --base-directory docs --output-path _site --force",
    "serve": "cd _site; python3 -m http.server 8202"
  }
}
